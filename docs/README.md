# Foro web documentation

Long-form docs live in the **Dira** workspace `foro` (MCP `dira`). Start at `docs/web/readme.md` there (hub: `docs/README.md`).

| Area | Dira path |
|------|-----------|
| Modules | `docs/web/02-modules/` |
| Roles | `docs/web/01-roles/` |
| Schema contracts | `docs/web/03-database/` (prefer live MySQL MCP if they disagree) |
| Plans / progress | `docs/web/plans/` |
| API contracts | `docs/api/api.md` |

Do not add new product docs in this folder. Update Dira instead (`search_docs`, then `edit_file` / `write_file`).
